---
description: Ingest a URL or file into resources
---

Load the `resources` skill and `resources/ingest` sub-skill, then execute **Workflow F** on the specified source.

Arguments: `/r-ingest <url-or-file-path>`. If no argument provided, ask for the URL or file path.

$ARGUMENTS
